# """Local-only Django settings overrides.

# This file is intentionally ignored by git (see .gitignore). Use it to
# declare any settings or helper imports that you do NOT want committed.
# Example usage:

#     from dotenv import load_dotenv
#     load_dotenv()

#     API_URL = "http://localhost:8000"

# Because the file is optional, breathecode/settings.py will silently skip the
# import if it does not exist.
# """

# try:
#     from dotenv import load_dotenv  # noqa: F401

#     load_dotenv()
# except ModuleNotFoundError:
#     pass

